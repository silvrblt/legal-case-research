# 更新日志

本项目版本号遵循「功能里程碑」粒度，非严格 SemVer。

## v1.1.7 —— 许可证变更：MIT → PolyForm Noncommercial 1.0.0（禁止商用）
- **重新授权**：项目许可证由 MIT 改为 **PolyForm Noncommercial 1.0.0**——源代码保持可见、
  允许学习 / 研究 / 教育 / 非营利使用，但**禁止任何商业用途**；商业授权请单独联系版权人。
- README 许可证徽标同步更新。注：历史以 MIT 发布版本的既有授权不因本次变更而追溯撤销。

## v1.1.6 —— 时效结构增「顺位内」子集（实案模式深挖候选的时效真值）
- **顺位内时效**：有 `顺位法院.json` 时，`时效结构` 增 `顺位内` 子集——仅统计命中四顺位
  目标法院的案件（与顺位覆盖同口径），这才是「类案规则」章深挖候选的近三年/逾三年真值；
  全池统计仍保留供全景章参考。tiers 加载上提、与顺位覆盖共用一次解析。
- 新增 2 项测试（顺位内子集计数 + 非实案模式不产出该子集），44 项全绿。

## v1.1.5 —— 近三年优先硬化为可核查统计（check_coverage 时效结构）
- **新增 `时效结构` 节**：`check_coverage.py` 计算近三年（基准日=生成日上溯三年）占比、
  逾三年清单（案号+审结日+标题，供深挖位降级判断）、未知日期数，写入 `07_coverage.json`
  并打印摘要——把"近三年优先"从 SKILL 软指令变成有数据背书的可核查行为。
- **指导性案例豁免**：依第四条收尾句"除指导性案例以外"，指导性案例单列、不计入时效升降
  （应当参照，不因年代久而降级）；公报案例/典型案例不豁免。
- SKILL 实案检索步骤接入此统计；新增 _recency 正反测试（固定基准日抗日期漂移），42 项全绿。
- 边界：仍是"优先"非"限于"——逾三年案不删，全景章保留旧规案作新旧规对照。

## v1.1.4 —— 第五/六/八/九/十一条逐字复核：修正第六条引注误植
- **全面复核**：以最高法官网全文逐条核对实案模式对法发〔2020〕24号的编码。
  第八条（报告要素）、第九条（应当参照/可以参考）、第十一条（"检索到的类案存在法律适用
  不一致的"）三条**逐字无误**；第五条三种检索方法补注于 keyword-strategy。
- **修正第六条引注误植**：三要素（基本事实/争议焦点/法律适用问题）的**定义在第一条**
  （原文"…等方面具有相似性"，"等"表非穷尽），**第六条**是"将待决案件与检索结果进行相似性
  识别和比对"的操作要求。此前把三要素直接归给第六条，混了"定义条"与"比对条"。已在
  relevance-screening、SKILL、ROADMAP、demo 实案报告四处校正为"第一条定义 + 第六条比对"。

## v1.1.3 —— 实案模式补全法发〔2020〕24号第四条收尾句
- **补「近三年优先」检索规则**：第四条收尾句「除指导性案例以外，优先检索近三年的案例
  或者案件；已经在前一顺位中检索到类案的，可以不再进行检索」原只编码了后半句（前一顺位
  命中可不再检索），漏了前半句（近三年优先）。经最高法官网逐字复核后补齐两半——
  SKILL 四顺位说明、实案检索步骤、报告第八条附录指引、模板均写明；demo 实案报告检索方法
  项同步体现。四档检索范围本身经复核**逐字无误**。

## v1.1.2 —— 「」豁免通道哨兵：不核验 ≠ 不可见
- **哨兵 WARN**：「」按纪律只装短术语、不进引文核验——把编造"判决原话"包进「」即可绕过
  核验层（prompt 级纪律的固有缺口）。verify_report 新增哨兵：≥20 字、或含裁判语体特征
  （本院认为/应当/构成等）的「」片段提示人工抽核；命中语料者自动豁免。**不阻断**（即使
  `--strict-quotes`），只把豁免通道置于可见状态。
- **边界声明补窄**：README/docstring 同步写明「」标记的归纳语亦不在核验范围——宁可宣称窄、
  不可宣称宽。模板 §2c 增反向禁令：不得把判决原话包进「」逃避核验。
- 哨兵首扫即抓到 demo 中 5 条超长「」归纳语，已去引号归位；新增 2 项测试
  （哨兵正反用例 + demo 无警示回归），41 项全绿。

## v1.1.1 —— 引文层补丁：旗标过滤 + 引号写作纪律 + CI strict 回归
- **修复**：verify_report 显式传报告文件名时 `--strict-quotes` 被当文件名导致必然 FAIL——
  构建报告清单前过滤 `--` 开头参数；新增回归测试。
- **引号写作纪律**（模板 §2c 固化）：弯引号“”专用于逐字引用（受核验），直角引号「」用于
  术语/归纳语（不核验）；核验正则相应只认弯引号。三套 demo 报告引号全部归位。
- **CI strict 回归保护**：两条冒烟管道的 verify 升级为 `--strict-quotes`，引文层从此有回归防线。
- **模糊≠逐字边界注明**：脚本 docstring 与 README 写明「模糊命中≈2/3 以上连续片段来自
  原文，不保证逐字一致」；未命中提示措辞同步校准。

## v1.1 —— 外部评审回应：引文核验层 + 编码抽检 + 诚实边界声明
针对外部评审五点批评逐一落实：
- **引文核验层（内容级抽检）**：verify_report 新增第二层——报告中引号内直接引文在判决
  全文语料（全池正文字段+output/原文/）归一化匹配（嵌套引号容错、分块模糊判定），
  逐字引用判决原话应当命中、**伪造"判决原话"会被标出**；未命中清单提示人工抽核，
  `--strict-quotes` 升级 FAIL。实测：真实原话精确命中、伪造引文未命中。
- **措辞收敛（诚实边界）**：README/SKILL/AGENTS 全面把"反幻觉被机器证明"收敛为
  "引用与引文可溯源性被机器证明"，明示**不能证明全部转述内容无误**，转述质量由编码
  抽检与人工检查点把关；英文 tagline 改 machine-verified citation traceability。
- **编码抽检机制**：spot_check_coding.py 随机抽 ≥15%（至少 3 件）生成"编码结论 vs
  原文摘录"对照复核单，随检查点交用户逐件确认留痕——内容分析编码者信度的工程化替代
  （双跑 Kappa 入 ROADMAP）；SKILL 设为强制工序。
- **编码落盘标准化**：apply_coding.py——AI 产纯 JSON 编码合并入 05，受保护 MCP 原始
  字段拒绝改写（字段保全机器强制），收编"每次研究手写脚本"的手工环节。
- **认知负荷与兼容性**：SKILL 顶部新增"铁律速查卡"（10 条×违反后果）；AGENTS 兼容性
  表述收敛为"架构设计目标，判断层仅在 Claude Opus 级完整验证"；跨模型 eval 入 ROADMAP。
- **召回率天花板明示**：README 写明"量化不等于消除——单查询 10 条上限是结构性约束"。
- 新增 2 项测试（37 项全绿）。

## v1.0 —— 双模式定版收官：学理模式实战 + 自动阀门 + 全面体检
- **学理研究模式首次完整实战**：《证券虚假陈述-裁判规则研究报告-20260611》——零新增 token
  复用 475 条底座，38 件深度编码核心判决（11 省、新旧规各半），制度变迁总览＋五维四段
  （理论展开/学理争议对照/演进）＋全国地域图＋争点×地域热力矩阵＋13 分歧争点；
  典型案例名录联网核对（8 命中 3 缺口如实呈报）；29 个引用案号全部溯源。
- **集团案自动检测阀门**（架构定版）：案件形态不再由用户指定——`fold_group_cases.py`
  检索后自动检测「同被告×同法院」成串相似判决（组规模≥4 触发），三级分组键折叠核心判决，
  检查点 2 交用户校核。学理实战临时脚本升格为正式模块。
- **字体规范**：Word 渲染全面"西文 Times New Roman＋中文宋体/黑体"（FT 字体助手，21 处统一）。
- **地域图表模块化**：`render_region_charts.py`（幂等，analytics 重跑后回填 manifest）。
- **遗留修复**：derive_region 全国化（原仅认京沪致全国样本法院地失真）；verify_report
  系列合并案号前缀匹配。
- **模块地图**：README 列 13 个技术模块索引；AGENTS.md 重写为两模式×两形态完整命令序列
  （fork 后任何 AI 照单执行）。
- **冗余清理**：删除零引用旧脚本 screen_cases.py / summarize_raw.py（功能已由
  fold_group_cases.py 取代）。35 项测试全绿。

## v0.9 —— 实案研究模式定版（甲股份证券虚假陈述实战打磨完成）
经一次完整实战（上海金融法院·实案×集团案）三轮返工打磨，实案模式产出规格定版，
"只要给案例即可复现同等效果"。定版要件全部固化于 SKILL 与 report-neutral 模板：
- **报告结构**：封面（两行标题+署名入页脚）→ 执行摘要 → 一、研究范围与方法（含案情要素
  拆解映射表、相似性量化评分方法、新旧规分界、双轨口径）→ 二、类案规则·四顺位（每件深挖案
  **六件套**：基本案情/被告抗辩逐条/法院裁判逻辑逐条对应/**证据对照表**/相似性评分表/参照
  要点+第九条效力标注；单争点参照表；背景提示）→ 三、裁判规则全景（五维四段+**理论展开**+
  新旧规演进+全国地域分布图）→ 四、综合分析（规则地图+控辩要点+**控辩证据准备清单**）→
  五、结论 → 附录（评分索引+第八条检索过程说明）。
- **量化与可信**：三要素相似性评分量表（≥90 深挖/80–89 深挖/60–79 单争点/<60 背景提示）；
  反幻觉校验+覆盖率自检+契约校验全过；原文导出 `output/原文/`（核心判决+演进对照案）。
- **格式纪律**：标题括号纪律（纯说明性括注删去）；内容感知列宽；统一冷蓝渐变图表
  （stacked_year 重写）；地域归省治本（normalize_province，金融法院→所在市）；
  地域图基于全国 03 全量判决书。
- 实战暴露并修复：金融法院省份解析、tag_tiers 顺位标注缺口、汇编条目校验误杀、
  单一分组分歧闸门误报、run_analytics 覆盖全国地域图。

## v0.8 —— 定版图景：单技能两开关 + 实案六件套/证据线 + 命名分化
- **项目总纲定版**：实案研究 = 裁判规则研究的 plus 版本，只多不少——裁判规则研究全部模块整体内嵌为实案报告主体，实案额外前置四顺位类案深挖，深度不降一分。
- **技能合并**：group-litigation-research 并入 case-research，成为单一技能**两个正交开关**（研究模式：学理/实案；案件形态：散案/集团案，证券虚假陈述为集团案示范样例）；四种组合均可。
- **命名分化**：学理模式产出 `<主题>-裁判规则研究报告-<日期>`；实案模式产出 `<案件类别>-类案检索报告-<日期>`；清单统一 `<类别>-类案检索清单-<日期>`。verify_report 同步识别两种命名。
- **实案深挖五件套→六件套**：新增**证据对照表**（争议焦点|原告举证|被告举证|法院采信，未详列处如实标注）；实案控辩要点表后必配**控辩证据准备清单**（从法院实际采信证据归纳）。
- **全景章理论展开**：每节"①争点界定"必须含 100–200 字制度功能与理论背景（信息过滤器/欺诈市场理论/看门人机制等），不得一句带过即进案例。
- **图表风格统一**：stacked_year 重写为冷蓝系渐变分段+柔投影+白色分隔线，与全套图表同一视觉语言；RESULT_COLORS 去灰。

## v0.7 —— 双模式：实案研究 / 学理研究（二期）
- **双研究模式**：case-research 技能开工时选择——学理研究（全国检索）或实案研究（指定核心法院，按法发〔2020〕24号第四条四顺位检索：指导性案例→最高法→本省高院→上一级及本院）。
- **指导性案例本地数据集** `data/guiding_cases/`（263 案全文）：`fetch_guiding_cases.py` 自最高法官网合规限速抓取（自动翻页 15 页、断点续传、失败重试），含关键词/裁判要点/基本案情/裁判结果/裁判理由/相关法条结构化解析；官网未收录的 16 个编号（疑似废止下架）如实记录于数据集 README，引用前按第九条核查效力。
- **四顺位法院解析** `court_hierarchy.py` + `data/court_hierarchy.json`：直辖市多中院映射（朝阳→北京三中院等）、专门法院上诉关系、非直辖市通用规则推断；提议制——检查点 1 必须用户确认后落盘 `顺位法院.json`。
- **相关度 A/B/C 方法论**：A 套高贴合（高相关候选，≈90%+ 标识）/B 套概念放大（中相关候选，≈80%+ 标识）/C 套兜底（仅不足时启用+特别提示）；终档按第六条三要素比对量表（基本事实/争议焦点/法律适用），每案记`相关度依据`；百分比明示为分级标识非计量值。
- **报告新结构**：「案情要素拆解与检索映射表」小节（要素×案情拆解×A/B 套词）；学理模式地域分布图 `region_dist` + 争点×地域观点热力矩阵 `issue_region`（描述性，受闸门约束）且不设控辩表；实案模式新增「类案规则（依据《类案检索规定》）」专章（逐顺位三要素比对+裁判逻辑拆解+应当参照/可以参考效力标注）、附录满足第八条要素。
- `check_coverage.py` 顺位覆盖节落地（读 `顺位法院.json` 逐顺位统计命中与缺口）。
- 新增 6 项二期测试（33 项全绿）。

## v0.6 —— 质量门三件套（数据层与质量层，双模式一期）
- **检索覆盖率自检** `scripts/check_coverage.py` → `07_coverage.json`：关键词命中矩阵（含"独有命中"边际贡献）、案件等级/法院层级分布、去重审计、典型案例名录命中/缺口表（`名录.json`）——把"完整性不可保证"变成量化缺口表，亦是法发〔2020〕24号第八条"方法/结果"要素的底稿；为实案模式预留"顺位覆盖"节。
- **管道数据契约校验** `scripts/common/pipeline_schema.py` + `scripts/validate_pipeline.py`：03–06 逐段校验字段类型/枚举/焦点名拼写一致性（拼写漂移会导致聚合分裂），宽松白名单设计（新字段不报错，二期零返工）；两套 run_analytics 内置 warn 模式复查。
- **分歧检测一等产出（数据层）** `scripts/common/divergence.py`：06 新增 `分歧地图`（同争点对立立场并存清单 + 各立场代表案对 + 地域观察 + 闸门措辞，法发〔2020〕24号第十一条情形显性化）；两版 Excel 新增「裁判分歧清单」sheet。
- **ROADMAP.md**：固化二期"双模式（实案研究/学理研究）"完整施工图（四顺位检索引擎、相关度 A/B/C 方法论、案情要素拆解映射表、地域观点矩阵、第八/九条合规等）。
- 《类案检索指导意见》文号经法宝法规 MCP 核验：法发〔2020〕24号，现行有效。
- 新增 10 项质量门测试（27 项全绿）；CI 两个 demo 冒烟接入 validate + coverage。

## v0.5 —— 工作流再抽象 + 通用化 + 命名规范
- **工作流分类重构**：证券虚假陈述专题（`securities-misrep-research`）抽象为通用 **`group-litigation-research`（集团/群体性诉讼）** 工作流——核心区别是"从一事件派生的大量平行判决中按主体+事件分组、筛出核心实质判决"；证券虚假陈述降为内置示范样例。
- **固定报告/清单命名规范**：`<案件类别>-类案检索报告/清单-<YYYYMMDD>`。
- **通用性**：自带数据模式（`import_cases.py` + 输入数据契约，无需法宝订阅）；`AGENTS.md` 工具无关运行指南（不限 Claude）。
- **内外分离**：个人画像 `CLAUDE.md` → `CLAUDE.example.md` 模板 + gitignore。
- **展示样板**升级为证券虚假陈述集团诉讼合成 demo（README hero）。

## v0.4 —— 开源加固（可展示 / 可复现 / 可证明）
- **新增 LICENSE（MIT）**，使项目可被合法使用与二次开发。
- **新增脱敏演示样本** `examples/`：合成一份与 MCP 返回结构一致的小样本（虚构案号/当事人，见 `DISCLAIMER.md`），**无需法宝 Token 即可跑通整条管道**并产出报告/Excel/图表，作为 GitHub 展示与测试夹具。
- **新增反幻觉收尾自检** `scripts/verify_report.py`：扫描成品报告每一个案号，逐一比对样本池，引用了样本池外的案号即报错——把"承诺不编案号"升级为"机器证明可溯源"。已接入两套技能的收尾流程与 CI。
- **新增自动化测试与 CI**：`tests/` 下 pytest 覆盖公共派生、样本量自适应闸门（含"小样本不作分歧推断"回归保护）、Excel 实质列非空、反幻觉校验正反用例；GitHub Actions 每次推送/PR 运行测试 + 演示管道冒烟 + 反幻觉校验。
- README 增补徽章、英文简介、Demo 展示区（图表内联）、可靠性章节。

## v0.3 —— 通用 case-research 深化至 v2（对齐证券专题）
- 新增动态争点体系、样本量自适应闸门（`stats_guard`）、统一主题出图（`chart_theme`）、全文驱动的研报级三场景报告（脚注/图表/封面），与证券专题对齐共用层。

## v0.2 —— 新增证券虚假陈述专题
- 在通用版基础上新增 `securities-misrep-research` 技能；公共派生函数抽到 `scripts/common/`，两套工作流共享一处维护。

## v0.1 —— 北大法宝类案研究工作流
- 通用 case-research 技能：三步两检查点流程、双轨分流、六维统计、Excel + 报告产出。
